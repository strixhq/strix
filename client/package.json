{
  "name": "@strix/client",
  "version": "0.0.4",
  "main": "mod.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx esbuild ./mod.ts --bundle --minify --platform=browser --outfile=dist/mod.js --format=esm"
  },
  "author": "ihasq",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "@ihasq/random": "npm:@jsr/ihasq__random@^0.1.5"
  }
}
