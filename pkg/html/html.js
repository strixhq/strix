import { tmp } from "../../mod/tmp.js";

/**
 * 
 * @param { TemplateStringsArray } str 
 * @param  { any[] } val 
 * @returns { Function }
 */

const html = tmp(0);

/**
 * 
 * @param { TemplateStringsArray } str 
 * @param  { any[] } val 
 * @returns { Function }
 */

html.new = tmp(1);

export default html;
