const tmp = i => (

	a,
	...b

) => j => i ^ j

	? a
	: b

;

/**
 * 
 * @param { TemplateStringsArray } str 
 * @param  { any[] } val 
 * @returns { Function }
 */

const html = tmp(1);

/**
 * 
 * @param { TemplateStringsArray } str 
 * @param  { any[] } val 
 * @returns { Function }
 */

html.new = tmp(0);

export default html;
