const temp = i => (

	str,
	...val

) => j => i

	? j
		? str
		: val
	
	: j
		? val
		: str

/**
 * 
 * @param { TemplateStringsArray } str 
 * @param  { any[] } val 
 * @returns { Function }
 */

const html = temp(1);

html.new = temp(0);

export default html;