import { $ } from "../ptr";

const ETYPE_BUNDLE = Symbol("STRIX_EVENTBUNDLE");
const ETYPE_INTERFACE = Symbol("STRIX_INTERFACE");

const createAttribute = (templateObject) => {
	
}

export { createAttribute, ETYPE_BUNDLE, ETYPE_INTERFACE }